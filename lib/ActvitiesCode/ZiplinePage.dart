import 'package:flutter/material.dart';

class ZiplinePage extends StatelessWidget {
  const ZiplinePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
