import 'package:flutter/material.dart';

class SialkotPage extends StatelessWidget {
  const SialkotPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
